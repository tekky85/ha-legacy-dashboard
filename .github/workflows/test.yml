name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Abhängigkeiten installieren
        run: npm ci

      - name: JavaScript-Syntax prüfen
        run: find src test -type f -name '*.js' -exec node --check '{}' ';'

      - name: Shell-Syntax prüfen
        run: find deploy ha_legacy_dashboard -type f -name '*.sh' -exec sh -n '{}' ';'

      - name: Mock- und Integrationstests
        run: npm test
